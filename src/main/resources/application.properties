spring.application.name=ms-agendamento
server.port=${PORT:8080}

# ===== LAZY INITIALIZATION - Critical for Cloud Run fast startup =====
spring.main.lazy-initialization=true

# ===== Exclude JPA/DataSource auto-config so app starts without DB =====
# JPA is configured manually in DataSourceLazyConfig and JpaLazyConfig
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\
  org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,\
  org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration

# ===== CloudSQL Configuration (read by manual DataSource bean) =====
spring.datasource.url=jdbc:mysql:///feedback?cloudSqlInstance=fiap-474820:us-central1:ms-agendamento&socketFactory=com.google.cloud.sql.mysql.SocketFactory&useSSL=false&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# RabbitMQ
spring.rabbitmq.host=${RABBIT_HOST}
spring.rabbitmq.port=${RABBIT_PORT}
spring.rabbitmq.username=${RABBIT_USERNAME}
spring.rabbitmq.password=${RABBIT_PASSWORD}
spring.rabbitmq.virtual-host=/

# Habilita execucao automatica de scripts SQL na inicialização
spring.sql.init.mode=always

# Arquivos SQL a serem executados (executados na ordem)
spring.sql.init.data-locations=\
classpath:db/statusConsulta.sql,\
classpath:db/statusNotificacao.sql

# ===== Disable health checks that require DB =====
management.health.db.enabled=false
